import { preprocess } from './preprocess'
import { common } from './common'
import { menu } from './menu'
import { querySelectorHelper } from './query-selector-helper'
import { credential } from './credential'
import { plugin } from './plugin'

export const en = {
  preprocess,
  common,
  menu,
  querySelectorHelper,
  credential,
  plugin,
}
